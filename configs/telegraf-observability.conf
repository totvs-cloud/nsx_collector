# Telegraf - Observabilidade do nsx-collector
# Coleta metricas do coletor NSX e escreve no bucket "observability"
# Deploy: /etc/telegraf/telegraf.d/nsx-collector-observability.conf

[[inputs.prometheus]]
  urls = ["http://localhost:9101/metrics"]
  metric_version = 1
  interval = "30s"
